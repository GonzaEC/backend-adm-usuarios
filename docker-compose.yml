# de uso local
services:
  db-local:
    image: postgres:15-alpine
    container_name: postgres_sip_2026
    environment:
      POSTGRES_USER: dev_user
      POSTGRES_PASSWORD: dev_password
      POSTGRES_DB: sip_project
    ports:
      - "5432:5432"